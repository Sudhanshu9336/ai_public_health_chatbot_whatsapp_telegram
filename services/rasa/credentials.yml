rest:
  # FastAPI will call the REST webhook.
  # POST /webhooks/rest/webhook

# twilio:
#   account_sid: ${TWILIO_ACCOUNT_SID}
#   auth_token: ${TWILIO_AUTH_TOKEN}
#   twilio_number: ${TWILIO_FROM_SMS}
